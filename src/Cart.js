import { render } from "@testing-library/react";
import React from "react";

const Cart = (props) => {
    const cart = props;

    return (
        <div>
            Cart!
        </div>
    )
}

export default Cart;